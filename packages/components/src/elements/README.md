# STRATA Elements

The public component catalog is intentionally composed of small Web Components. Each element uses the STRATA foundations and the `strata-*` namespace.

The historical `wc-*` namespace exists only in `legacy.ts` for migration compatibility.
