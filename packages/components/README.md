# @strata/components

STRATA Web Components built with Lit.

## Icons

Use `iconify-icon` directly:

```html
<iconify-icon icon="carbon:add"></iconify-icon>
```

`@iconify-json/carbon` is the only permitted icon collection. The collection is registered locally so application rendering does not depend on the public Iconify API.
