# STRATA Scale Law

Physical size is the input to structural weight. `micro < small < medium < large < hero`; radius, border and elevation must be monotonically non-decreasing across those classes. Importance and semantic color cannot override physical scale.
