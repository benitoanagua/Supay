# Releases

STRATA uses Semantic Versioning and Changesets. Breaking public API changes are MAJOR, additive backward-compatible features are MINOR, and fixes are PATCH.

Every release should pass lint, typecheck, contract tests, accessibility tests, visual contract tests and production Storybook build.
