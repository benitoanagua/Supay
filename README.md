# Supay Elements

Web components built with Lit, Tailwind CSS, and Material Design 3.

## Development

```bash
pnpm install
pnpm dev
pnpm storybook
```

## Build

```bash
pnpm build
pnpm build-storybook
```

The component source lives in `src/` and Storybook configuration lives in `.storybook/`.
